import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:dailymedicaltrivia2/main.dart';
import 'package:dailymedicaltrivia2/ui/dialogs/StartLevelDialog.dart';
import 'package:dailymedicaltrivia2/model/Level.dart';

void main() {
  /*
  testWidgets('pressing play sends you to quiz page',
      (WidgetTester tester) async {
        await tester.pumpWidget(MaterialApp(home: StartLevelDialog(Level())));
        await tester.tap(find.byType(FlatButton).first);
        await tester.pumpAndSettle();
        expect(find.byType(Qui), findsOneWidget);
  });
  */
}
